<li class="nav-item tamara-tab-wrapper" data-method-id="${paymentOption.ID}">
    <a class="nav-link tamara-tab" data-toggle="tab" href="${'#' + paymentOption.ID}-content" role="tab">
        <img class="tamara-option"
                src="${paymentOption.image.getURL()}"
                height="22"
                alt="${paymentOption.name}"
                title="${paymentOption.name}"
        > <span style="color: #6dc8bf;">${paymentOption.name}</span>
    </a>
</li>
